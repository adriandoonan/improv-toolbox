---
import Drawer from "../components/Drawer.astro";
const {
  title = "Improv Toolbox",
  description = "Exercises, warmups, forms, and show tools.",
  themeColor = "#0f172a",
} = Astro.props;
const drawerId = "primary-navigation";
const navLinks = [
  {
    href: "/",
    label: "Home",
    icon:
      "<svg width='24' height='24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><path d='M4 11.5 12 4l8 7.5'/><path d='M6 10v10h5v-5h6v5h5V10'/></svg>",
  },
  {
    href: "/exercises",
    label: "Exercises",
    icon:
      "<svg width='24' height='24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round'><circle cx='12' cy='12' r='9'/><path d='M8 12h8'/></svg>",
  },
  {
    href: "/forms",
    label: "Forms",
    icon:
      "<svg width='24' height='24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><rect x='4' y='4' width='16' height='16' rx='4'/><path d='M8 9h8M8 12h8M8 15h5'/></svg>",
  },
  {
    href: "/warmups",
    label: "Warmups",
    icon:
      "<svg width='24' height='24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round'><path d='M12 4v16'/><path d='M4 12h16'/></svg>",
  },
  {
    href: "/tools",
    label: "Tools",
    icon:
      "<svg width='24' height='24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'><path d='M12 3a5 5 0 0 0-5 5c0 .7.1 1.4.4 2l-3.6 3.6a2 2 0 0 0 2.8 2.8L10.2 13.8a5 5 0 0 0 6.4 0l3.6 3.6a2 2 0 0 0 2.8-2.8L19.4 10A5 5 0 0 0 12 3Z'/></svg>",
  },
];
const menuIcon =
  "<svg width='24' height='24' fill='none' stroke='currentColor' stroke-width='2' stroke-linecap='round'><path d='M4 7h16M4 12h16M4 17h16'/></svg>";
const currentPath = Astro.url.pathname;
const currentYear = new Date().getFullYear();
const isActive = (href: string) =>
  href === "/" ? currentPath === "/" : currentPath.startsWith(href);
---

<html lang="en" data-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <meta name="theme-color" content={themeColor} />
    <title>{title}</title>
    <meta name="description" content={description} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Epilogue:wght@600;700&family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/styles/theme.css" />
    <link rel="stylesheet" href="/styles/every.css" />

    <link rel="manifest" href="/manifest.webmanifest" />
    <link rel="icon" href="/icons/icon-192.png" sizes="192x192" />
    <link rel="apple-touch-icon" href="/icons/icon-192.png" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="mobile-web-app-capable" content="yes" />
  </head>
  <body>
    <header class="app-bar" role="banner">
      <div class="app-bar__inner">
        <div class="app-bar__brand">
          <button
            type="button"
            class="app-nav-trigger"
            aria-label="Open navigation menu"
            data-drawer-target={drawerId}
          >
            <span class="sr-only">Open navigation menu</span>
            <span class="app-nav-trigger__icon" set:html={menuIcon} />
          </button>
          <div class="app-identity">
            <a class="app-logo" href="/">Improv Toolbox</a>
            <span class="app-tagline">Warmups · Forms · Tools</span>
          </div>
        </div>
        <div class="app-bar__actions">
          <a class="app-cta" href="/tools/timer">Quick timer</a>
        </div>
      </div>
      <nav class="tab-nav" aria-label="Primary navigation">
        {
          navLinks.map((link) => (
            <a
              href={link.href}
              aria-current={isActive(link.href) ? "page" : undefined}
            >
              {link.label}
            </a>
          ))
        }
      </nav>
    </header>
    <main class="page-shell stack">
      <slot />
    </main>
    <nav class="bottom-nav" aria-label="Primary navigation">
      {
        navLinks.map((link) => (
          <a
            href={link.href}
            class={`bottom-nav__item${
              isActive(link.href) ? " bottom-nav__item--active" : ""
            }`}
            aria-current={isActive(link.href) ? "page" : undefined}
          >
            {link.icon && (
              <span class="bottom-nav__icon" set:html={link.icon} />
            )}
            <span class="bottom-nav__label">{link.label}</span>
          </a>
        ))
      }
      <button
        type="button"
        class="bottom-nav__item bottom-nav__item--drawer"
        data-drawer-target={drawerId}
        aria-label="Open navigation menu"
      >
        <span class="bottom-nav__icon" set:html={menuIcon} />
        <span class="bottom-nav__label">Menu</span>
      </button>
    </nav>
    <Drawer id={drawerId} title="Navigate" links={navLinks}>
      <p class="app-tagline">Plan, teach, and play on the fly.</p>
    </Drawer>
    <footer class="site-footer">
      <small>© {currentYear} Improv Toolbox Collective</small>
    </footer>

    <script>
      if ("serviceWorker" in navigator) {
        navigator.serviceWorker.register("/sw.js").catch(() => {});
      }
    </script>
  </body>
</html>
